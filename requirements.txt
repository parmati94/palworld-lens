fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.11.5
colorlog==6.9.0
watchdog==5.0.3
sse-starlette==2.2.1
itsdangerous==2.2.0
Pillow==11.0.0
httpx==0.28.1
paramiko==3.5.0
# palworld-save-tools is installed in its own cache-busted Dockerfile layer so each
# build re-resolves @main (or a ref passed via the SAVETOOLS_REF build-arg) without a
# manual pin. Kept out of here to keep this layer cacheable.
