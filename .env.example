# Path to mounted Palworld save directory
SAVE_MOUNT_PATH=/app/saves

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Auto-watch save files and push updates to frontend (true/false)
# When enabled, the backend watches for save file changes and automatically
# pushes updates to the frontend via Server-Sent Events (no manual reload needed!)
ENABLE_AUTO_WATCH=true
