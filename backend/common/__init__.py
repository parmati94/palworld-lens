"""Shared backend helpers.

Deliberately empty: pal_ids, pal_icons, game_tables and constants are pure
python and are imported by the datagen scripts and the tests without the
backend's dependencies (colorlog etc.), so nothing may be imported here.
"""
